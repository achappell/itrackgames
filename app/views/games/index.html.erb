<h1>Listing games</h1>

<table>
  <tr>
    <th>Gametitle</th>
    <th>Platform</th>
  </tr>

<% @games.each do |game| %>
  <tr>
    <td><%= game.title %></td>
    <td><%= game.platform.name %></td>
  </tr>
<% end %>
</table>

<br />
