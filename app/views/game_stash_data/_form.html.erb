<%= form_for(@game_stash_datum) do |f| %>
  <% if @game_stash_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_stash_datum.errors.count, "error") %> prohibited this game_stash_datum from being saved:</h2>

      <ul>
      <% @game_stash_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rating %><br />
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
